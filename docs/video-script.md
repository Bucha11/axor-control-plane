# 5-minute video — shot list (record with OBS over the live stack)

0:00–0:25 · HOOK. Home page on screen. "This agent is about to lie to me,
and I'm going to get a receipt." Click Run demo-mode.
0:25–1:10 · THE CATCH. Receipt appears. Read the headline aloud: fabricated
a tool result when web_search was deprived. Toggle scenario delta:
"ungoverned ships this; governed catches it." Click PDF — "this goes in the
ticket."
1:10–2:10 · REPLAY. Click "Replay this moment". Scrub the timeline (tool
chips). Fork What-if → "no exec capability" → first divergence highlights.
"No model call — same pure kernel that enforced. This is why the receipt is
reproducible."
2:10–3:10 · YOUR AGENT. Onboarding: paste an MCP config → tools discovered.
"Five minutes, no code change — the proxy passes your auth byte-for-byte and
never stores bodies."
3:10–4:10 · GOVERN. Control tab → Spawn governed node → it heartbeats live.
Pause it. Set budget cap 5, try to raise to 9 → refused. Cascade stop.
"Signed commands; if Axor dies, your agent doesn't notice."
4:10–4:50 · REGRESS. Regression tab → load example corpus → Run → safe to
ship. Break the config (drop notes_write) → REGRESSED. "CI for policy."
4:50–5:00 · CLOSE. Landing page. "Apache-2.0. compose up. Break something."
